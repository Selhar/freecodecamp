"Public" folder is generated automatically, everything comes from "src".  

to minify your project, use `npm run prod` or `yarn run prod`, to start the webserver use `yarn run dev` or `npm run dev`, default port is 9000.
